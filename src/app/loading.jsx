import { PageLoading } from '@/components/Common/Loading';

/**
 * Default page loading component for Next.js
 * Uses the unified Loading component from Common
 */
const Loading = () => {
  return <PageLoading text='Loading' />;
};

export default Loading;
